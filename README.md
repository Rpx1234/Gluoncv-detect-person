# Gluoncv-detect-person
Gluoncv that detects and displays if there are any people in the webcams view. uses ssd_512_mobilenet1.0_voc model.

pip install argparse

pip install gluoncv

pip install opencv-python

pip install mxnet
